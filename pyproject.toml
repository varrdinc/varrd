[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "varrd"
dynamic = ["version"]
description = "Statistically validated trading edges across futures, equities, and crypto. See which edges are firing, get trade levels, or test any idea with real market data. MCP server + CLI + Python SDK."
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.9"
authors = [
    {name = "VARRD", email = "management@varrd.com"},
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Intended Audience :: Financial and Insurance Industry",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Office/Business :: Financial :: Investment",
]
keywords = ["trading", "quant", "edge", "backtest", "market-data", "mcp", "ai-trading", "algorithmic-trading", "event-study", "quantitative-finance", "futures", "crypto", "ai-agent", "model-context-protocol", "freqtrade", "jesse"]
dependencies = [
    "requests>=2.28",
    "pydantic>=2.0",
    "jinja2>=3.0",
]

[project.scripts]
varrd = "varrd.cli:main"

[project.urls]
Homepage = "https://varrd.com"
Repository = "https://github.com/varrdinc/varrd"
Documentation = "https://github.com/varrdinc/varrd#readme"
"Bug Tracker" = "https://github.com/varrdinc/varrd/issues"
"MCP Server" = "https://app.varrd.com/mcp"
Changelog = "https://github.com/varrdinc/varrd/releases"

[tool.setuptools.dynamic]
version = {attr = "varrd._version.__version__"}

[tool.setuptools.packages.find]
include = ["varrd*"]

[tool.setuptools.package-data]
"varrd.freqtrade.templates" = ["*.py"]
"varrd.jesse.templates" = ["*.py"]
